<%- include('../partials/header', { title: title }) %>

<style>
 body {
background-color: #0d1a26;
 color: #c0c0c0;
 padding: 30px 0;
 }
  
.card {
 background-color: #1c2e40;
 border: 1px solid #36454f;
 border-radius: 8px;
 box-shadow: 0 4px 8px rgba(0, 0, 0, 0.4);
 color: #ffffff;
 }

 .card-header {
 background-color: #36454f !important;
 color: #ffbf00 !important;
 border-radius: 8px 8px 0 0;
 font-weight: 700;
 font-size: 1.5rem;
 }

 .list-group-item {
 background-color: #2a415a;
 border: none;
 border-bottom: 1px solid #36454f;
 color: #ffffff;
 }

 pre {
 background-color: #0d1a26 !important;
 color: #ffbf00;
 font-weight: 600;
 border: 1px dashed #36454f;
 white-space: pre-wrap;
 word-break: break-all;
 }

 .badge.bg-success {
 background-color: #00704a !important; 
 font-size: 0.9em;
 padding: 0.4em 0.8em;
 }
 .badge.bg-danger {
 background-color: #8b0000 !important;
 font-size: 0.9em;
 padding: 0.4em 0.8em;
  }
 .card.border-danger {
 border: 2px solid #8b0000;
 }

.card-header.bg-danger {
 background-color: #8b0000 !important;
 color: #ffffff !important;
 }
 
 .btn-danger {
 background-color: #cc0000;
 border: none;
 font-weight: 700;
 transition: background-color 0.3s ease;
 text-transform: uppercase;
 }
.btn-danger:hover {
 background-color: #8b0000;
 }

 .btn-secondary {
 background-color: #ffbf00;
 border: none;
 color: #000000;
 font-weight: 600;
 text-transform: uppercase;
 transition: background-color 0.3s ease, transform 0.1s ease;
 }
.btn-secondary:hover {
 background-color: #e6a700;
 transform: translateY(-1px);
 }

 .list-group-item strong {
 color: #ffbf00;
 margin-right: 10px;
 }
</style>

<div class="container-fluid">
  <div class="row">
 <div class="col-md-8">
   <div class="card shadow-lg">
  <div class="card-header">
    <h3>Detail User & API Key 📝</h3>
  </div>
  <div class="card-body">
    <ul class="list-group list-group-flush">
   <li class="list-group-item">
     <strong>First Name:</strong> <%= detail.first_name %>
   </li>
   <li class="list-group-item">
     <strong>Last Name:</strong> <%= detail.last_name %>
   </li>
   <li class="list-group-item">
     <strong>Email:</strong> <%= detail.email %>
   </li>
   <li class="list-group-item">
     <strong>API Key:</strong>
     <pre class="p-2 rounded mt-1"><code><%= detail.api_key %></code></pre>
   </li>
   <li class="list-group-item d-flex justify-content-between align-items-center">
     <strong>Status:</strong>
     <div>
    <% if (detail.status === 'valid') { %>
      <span class="badge bg-success">Valid</span>
    <% } else { %>
      <span class="badge bg-danger">Invalid</span>
    <% } %>
     </div>
   </li>
   <li class="list-group-item">
     <strong>Start Date:</strong> <%= new Date(detail.start_date).toLocaleDateString('id-ID') %>
   </li>
   <li class="list-group-item">
     <strong>End Date:</strong> <%= new Date(detail.end_date).toLocaleDateString('id-ID') %>
   </li>
    </ul>
  </div>
   </div>
 </div>

 <div class="col-md-4 mt-3 mt-md-0">
   <div class="card shadow-lg border-danger">
  <div class="card-header bg-danger text-white">
    Aksi Berbahaya ⚠️
  </div>
  <div class="card-body">
    <p class="text-white">Menghapus API Key ini tidak dapat dibatalkan.</p>
    
    <form action="/admin/delete/<%= detail.id %>" method="POST" onsubmit="return confirm('APAKAH ANDA YAKIN INGIN MENGHAPUS KEY INI SECARA PERMANEN?');">
   <button type="submit" class="btn btn-danger w-100">
     HAPUS API KEY
   </button>
    </form>
  </div>
   </div>

   <a href="/admin/dashboard" class="btn btn-secondary w-100 mt-3">KEMBALI KE DASHBOARD</a>
 </div>
  </div>
</div>

<%- include('../partials/footer') %>